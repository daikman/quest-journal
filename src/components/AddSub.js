import React from 'react'
import { BsPlus as Plus } from "react-icons/bs"
export default function AddSub({ quest, index }) {
    return <span style={{cursor: "pointer"}} onClick={
        () => {
            // add sub
        }
    }><Plus /></span>
}